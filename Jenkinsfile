pipeline {
    agent any

    stages {
        stage('Checkout Test') {
            steps {
                echo 'Jenkinsfile loaded from GitHub!'
                sh 'ls -la'
            }
        }
    }
}